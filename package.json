{
  "name": "meta_ados",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w packages/database && npm run build -w apps/web && npm run build -w apps/api",
    "types": "npm run types -w packages/database"
  },
  "devDependencies": {
    "next": "^15.5.12",
    "supabase": "^2.76.12"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "eslint": "^4.1.1"
  }
}
